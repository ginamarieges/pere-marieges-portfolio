---
import Layout from '../layouts/Layout.astro';

export function getStaticPaths() {
  return [
    { params: { id: '1'}},
    { params: { id: '2'}},
    { params: { id: '3'}},

  ]
}

const {id} = Astro.params
---
<Layout title={`Project ${id}`}>
  <div>
    <h1>Esta es la página del proyecto num {id}</h1>
  </div>
</Layout>
